texto = "Before software can be reusable it first has to be usable"
texto2 = "You should prefer readability over performance in almost all cases"
texto3 = "Models are not right or wrong they are more or less useful"

print(len(texto3.split()))